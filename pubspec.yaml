name: login
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  #sdk: ">=2.18.0-170.0.dev <3.0.0"
  sdk: ">=3.0.0 <3.1.0"

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  http:
  intl:

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - images/welcome.jpg
    - images/logo-port-autonome-lome.png